export class Article{
    
    constructor(title, summary, content, comments){
        this.title = title;
        this.summary = summary;
        this.content = content;
        this.comments = comments;
    }
}